import React from "react";
//TODO map over results to add items to the list
//TODO add iconography

const ListItems = ({added}) => {
    return (
        <ul>
        <li>{added}</li>
    </ul>
    );
};

export default ListItems;